#include "training.h"


void comparebool (int a, int b)
{
    bool bl = a==b;
}

int main ()
{
    int a, b;
    cin >> a  >> b;
    comparebool (a, b);
    cout << comparebool << endl;
    return 0;
}